# Asset Preservation Digital Twin - Environment Configuration
# Copy this file to .env and update with your actual values
# NEVER commit .env to version control!

# ============================================
# InfluxDB Configuration (Time-series DB)
# ============================================
INFLUXDB_URL=http://localhost:8086
# Generate a secure token: openssl rand -base64 32
INFLUXDB_TOKEN=your-secure-influxdb-token-here
INFLUXDB_ORG=drone-org
INFLUXDB_BUCKET=telemetry

# ============================================
# PostgreSQL Configuration (Metadata DB)
# ============================================
POSTGRES_USER=drone_user
# Use a strong password in production
POSTGRES_PASSWORD=your-secure-postgres-password-here
POSTGRES_DB=drone_digital_twin
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# ============================================
# MQTT Broker Configuration
# ============================================
MQTT_BROKER=localhost
MQTT_PORT=1883
MQTT_TOPIC_TELEMETRY=drone/telemetry
# MQTT_USERNAME=username  # Uncomment if using authentication
# MQTT_PASSWORD=password  # Uncomment if using authentication

# ============================================
# Redis Configuration (Caching)
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=password  # Uncomment if Redis requires auth

# ============================================
# Application Settings
# ============================================
LOG_LEVEL=INFO
API_PORT=8000
# API_HOST=0.0.0.0  # Uncomment for production deployment

# ============================================
# Development/Debug Settings
# ============================================
# DEBUG=False
# ENVIRONMENT=development  # development, staging, production

# ============================================
# Optional: External Services
# ============================================
# AWS_ACCESS_KEY_ID=your-aws-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret
# AWS_REGION=us-east-1
# S3_BUCKET=drone-logs

# ============================================
# Security Notes
# ============================================
# 1. Generate strong random tokens for INFLUXDB_TOKEN
# 2. Use different passwords for each environment
# 3. Rotate credentials regularly
# 4. Never commit .env file to git
# 5. Use a secrets manager in production (AWS Secrets Manager, Azure Key Vault, etc.)
